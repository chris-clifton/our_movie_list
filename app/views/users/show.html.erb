<div>
  <h1> <%= @user.name %> </h1>

  <% if @user.lists.any? %>
    <h3> Lists (<%= @user.lists.count %>) </h3>
    <ol>
      <%= render @lists %>
    <ol>
    <%= will_paginate @lists %>
  <% end %>
</div>
